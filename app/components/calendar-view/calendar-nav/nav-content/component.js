import Component from '@ember/component';
import ComponentBaseMixin from 'calendar-demo/mixins/component-base';

const CalendarNavContentComponent = Component.extend(ComponentBaseMixin, {
  classPrefix: 'calendar-nav__content',
});

export default CalendarNavContentComponent;
