import Component from '@ember/component';
import ComponentBaseMixin from 'calendar-demo/mixins/component-base';

const CalendarHeroComponent = Component.extend(ComponentBaseMixin, {
  classPrefix: 'calendar-hero',
});

export default CalendarHeroComponent;
